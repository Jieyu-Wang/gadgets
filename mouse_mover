# To move your mouse and prevent screen lock
import pyautogui as pa
import time
start = time.time()
while 1==1:
    x, y = pa.position()
    pa.moveTo(x+1, y+1)
    print(f"{int(time.time()-start)}s")
    time.sleep(200)
